# src package -- the local, offline desktop game (single-machine
# single-/two-player). Imports the shared, server-critical engine/,
# shared/, controllers/, input/ packages from repo root rather than
# duplicating them -- see src/core/'s own module docstring for why.
