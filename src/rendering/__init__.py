# src.rendering -- the OpenCV presentation layer (board/piece drawing,
# animation, mouse/key input wiring). Moved from ui/graphics/ -- pure
# rendering, no game-rules logic of its own (see src/core/ for that).
